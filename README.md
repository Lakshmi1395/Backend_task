# Task
## Initial setup

The below project initialization steps helps us to get started with the development. It is advised to use the proper commands from the menu item.

1. Run `npm i .`
2. Run `npm run docker`
3. Run `npm run db`
4. Run `npm run start`
